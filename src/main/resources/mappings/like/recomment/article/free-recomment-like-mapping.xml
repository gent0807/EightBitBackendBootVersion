<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FreeReCommentLikeMyBatisDAO">

    <select id="getReCommentLikes" resultType="string">
        select liker from freeReCommentLike where reCommenter=#{author} and regdate=#{regdate}
    </select>

    <insert id="insertReCommentLike">
        insert into freeReCommentLike values (#{liker},#{author},#{regdate})
    </insert>

    <delete id="deleteReCommentLike">
        delete from freeReCommentLike where liker=#{liker} and reCommenter=#{author} and regdate=#{regdate}
    </delete>

</mapper>