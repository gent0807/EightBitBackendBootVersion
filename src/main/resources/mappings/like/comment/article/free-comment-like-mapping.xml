<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FreeCommentLikeMyBatisDAO">


    <select id="getReplyLikes" resultType="string">
        select liker from freeReplyLike where replyer=#{author} and regdate=#{regdate}
    </select>


    <insert id="insertReplyLike">
        insert into freeReplyLike values (#{liker},#{author},#{regdate})
    </insert>


    <delete id="deleteReplyLike">
        delete from freeReplyLike where liker=#{liker} and replyer=#{author} and regdate=#{regdate}
    </delete>


</mapper>