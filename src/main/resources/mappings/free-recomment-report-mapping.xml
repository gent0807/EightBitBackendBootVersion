<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FreeReCommentReportMyBatisDAO">

    <select id="getReportCount" resultType="_int">
        select count(*) from freeReCommentReport where reporter=#{reporter} and reCommenter=#{reCommenter}
                                                  and regdate=#{regdate} and report=#{report}
    </select>

    <insert id="insertReport">
        insert into freerecommentreport values(#{reporter},#{reCommenter},#{regdate},#{report})
    </insert>

    <delete id="deleteReport">
        delete from freerecommentreport where reporter=#{reporter} and reCommenter=#{reCommenter}
                                        and regdate=#{regdate} and report={report}
    </delete>
</mapper>