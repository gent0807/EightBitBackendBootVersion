<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FreeArticleReportMyBatisDAO">
    <select id="getReportCount" resultType="_int">
        select count(*) from freeArticleReport where reporter=#{reporter} and writer=#{writer}
                                                  and regdate=#{regdate} and report=#{report}
    </select>
    <insert id="insertReport">
        insert into freearticlereport values(#{reporter},#{writer},#{regdate},#{report})
    </insert>

    <delete id="deleteReport">
        delete from freearticlereport where reporter=#{reporter} and writer=#{writer}
                                      and regdate=#{regdate} and report={report}
    </delete>
</mapper>