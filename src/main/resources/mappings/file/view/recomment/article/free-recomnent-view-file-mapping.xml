<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FreeReCommentViewFileMyBatisDAO">

    <select id="getViewFileList" resultType="uploadFile">
        select * from freeReCommentFileView where uploader=#{uploader} and regdate=#{regdate}
    </select>

    <select id="getViewFile" resultType="uploadFile">
        select * from freeReCommentFileView where id=#{id} and uploader=#{uploader} and regdate=#{regdate}
    </select>

    <insert id="insertArticleViewFile">
        insert into freeReCommmentFileView(uploader, regdate, storeFilename, uploadFilename) values(#{uploader}, #{regdate}, #{storeFilename}, #{uploadFilename})
    </insert>

    <delete id="deleteArticleViewFile">
        delete from freeReCommentFileView where id=#{id} and uploader=#{uploader} and regdate=#{regdate}
    </delete>

</mapper>
